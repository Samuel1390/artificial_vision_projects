# Bienvenido al repositorio de projectos de vision artificial

Este repositorio contiene una serie de projectos de vision artificial desarrollados en Python utilizando OpenCV y MediaPipe.

# Instalación de dependencias

```bash
pip install opencv-python mediapipe numpy matplotlib
```
cada proyecto explica como usarse

Sígueme en Instagram como @samuel_nelo.py ahí estaré subiendo cada proyecto nuevo que haga.
